[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "topols_core"
version = "0.1.0"
description = "Rust core of TopoLS: the layer-by-layer MCTS embedding"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "topols_core"
